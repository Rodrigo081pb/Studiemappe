// Definição de escopo

let result = (2 + 5) * 2;
// Como coloquei entre parenteses a operação de adição então vai ser a primeira a ser feita e somente depois vai ser multiplicado gerando o resultado que é 14
console.log(result);

let resultado = (50 + 1) * (2 + 4 / 2);

console.log(resultado);

let calc = 3 * 2 + 8;

console.log(calc);
